namespace OpenClick.UI;

// Placeholder — replaced by the real MainForm during the UI build phase.
public sealed class MainForm : Form
{
    public MainForm()
    {
        Text = "OpenClick";
        ClientSize = new Size(540, 460);
    }
}
